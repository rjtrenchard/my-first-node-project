const r = 100;
const g = 200;
const b = 255;

const message = `color is ${r + g + b}`;

console.log(message);

const n = function () { 
    return "test";
}

const new_message = `test function = ${n()}`;

console.log(new_message);